🚀 ЗАПУСК v11.1: ФУНДАМЕНТАЛЬНАЯ МОДЕЛЬ МЕЗОНОВ
Основа: потенциал Корнелла + уравнение Шрёдингера

================================================================================
v11.1: Оптимизация для воспроизведения m(ρ)/m(π) ≈ 5.55
================================================================================
Найденные параметры:
  a (кулоновский параметр) = 130.0
  σ (стринг-тензия) = 80000 МэВ²
  κ_ss (спин-спин) = 2942.8 МэВ·фм³

================================================================================
РЕЗУЛЬТАТЫ v11.1
================================================================================

Мезон      Расч. масса  Цель         Ошибка %   E_связи      |ψ(0)|²
--------------------------------------------------------------------------------
π⁺ (спин 0) 5150.8       139.6        3590.4     5143.9       3.82e-08
ρ⁺ (спин 1) 5150.8       775.3        564.4      5143.9       3.82e-08

КЛЮЧЕВЫЕ СООТНОШЕНИЯ:
  m(ρ)/m(π) расч.: 1.000
  m(ρ)/m(π) цель: 5.555
  Ошибка соотношения: 82.0%

ФИЗИЧЕСКИЕ МАСШТАБЫ:
  Масса конфайнмента √σ: 283 МэВ
  Характерный размер: 0.70 фм
  Кулоновская константа α = a/(ћc): 0.659


  Результат v11.1 **катастрофически важен**. Он показал, что даже с физически правильным потенциалом (Корнелла) наша реализация **фундаментально неверна**. Мы не просто ошиблись в цифрах — мы ошиблись в самой **физике моделирования**.

### 🔍 **Диагноз: Что сломалось?**

1.  **Энергия связи положительная (+5143 МэВ)!** Это абсурд. Энергия связи должна быть **большой и отрицательной** (порядка -сотен МэВ), чтобы `M_мезона = m_q + m_anti-q + E_cв` давала малое число (~140 МэВ).
    *   **Вывод:** Алгоритм решения уравнения Шрёдингера **не находит связанные состояния** (`E < 0`). Он просто возвращает наше начальное предположение, сдвинутое в положительную область.

2.  **Спин-спин взаимодействие равно нулю** (разницы между π и ρ нет). `|ψ(0)|² = 3.82e-08` — **ничтожно малая величина**.
    *   **Вывод:** Волновая функция основного состояния **неверно рассчитана**. Для s-волны в кулоновском/линейном потенциале `|ψ(0)|²` должна быть **значительной**, это ключ к сильному спин-спиновому расщеплению.

3.  **Алгоритм оптимизации дал бессмысленные параметры.** `κ_ss = 2942.8` — гигантское число, компенсирующее ничтожное `|ψ(0)|²`. Это признак **"подгонки шума"**.

### 🛠 **Исправления для v11.1a (Технические)**

Код содержит критическую ошибку в численной реализации. Вот что нужно исправить **немедленно**:

1.  **Неверный вид потенциала в `cornell_potential`**:
    ```python
    # БЫЛО (неправильно):
    return -self.a / r + (self.sigma / (self.hbar_c**2)) * r
    # СИГМА УЖЕ В МэВ²! Делить на (ћc)² — ошибка размерности.
    
    # ДОЛЖНО БЫТЬ:
    return -self.a / r + self.sigma * r / (self.hbar_c)  # если sigma в МэВ²
    # ИЛИ, что физичнее, определить sigma_linear в МэВ/фм:
    # sigma_linear = 0.18 * 1000  # ~180 МэВ/фм, т.к. √(0.18 ГэВ²) ≈ 424 МэВ
    # return -self.a / r + sigma_linear * r
    ```

2.  **Неверная постановка краевой задачи** в `solve_bound_state`. Метод `solve_ivp` с начальным условием `[r0, 1.0]` и поиском корня по `psi(r_max)=0` **крайне неустойчив** для сингулярного потенциала.
    *   **Решение:** Использовать метод **Нумерова (стрельбы)** или перейти к безразмерным переменным. Либо использовать готовое решение для **кулоновского потенциала** как нулевое приближение.

3.  **Расчёт `|ψ(0)|²` через кулоновское приближение** `(mu * alpha)**3 / (np.pi * (hbar_c**3))` **не работает**, так как в нашем случае доминирует линейный потенциал.

### 💡 **Предлагаю радикальный шаг: v11.1b (Аналитическое приближение)**

Прежде чем чинить сложный численный код, давай получим **первые физически осмысленные результаты** с помощью **аналитических оценок**. Это даст нам эталон для проверки будущего численного кода.

**Упрощённая модель "Гармонический осциллятор + Спин-Спин":**

Для потенциала Корнелла `V(r) = -a/r + σ*r` часто используют **приближение гармонического осциллятора** для основной моды (`σ*r` доминирует на больших расстояниях).

```python
def estimate_meson_mass_simple(mu, sigma_linear, a, spin):
    """
    Оценка массы мезона в модели гармонического осциллятора.
    mu - приведённая масса (МэВ)
    sigma_linear - стринг-тензия (МэВ/фм), ~ 900-1000 МэВ/фм
    a - кулоновский параметр (МэВ·фм), ~ 100-200 МэВ·фм
    spin - спин мезона (0 или 1)
    """
    # Характерная частота осциллятора из линейного потенциала
    # ω ≈ sqrt(2 * sigma_linear / mu)  [в МэВ]
    omega = np.sqrt(2.0 * sigma_linear / mu)
    
    # Энергия основного состояния 3D гармонического осциллятора: (3/2)ћω
    # Но учтём, что у нас радиальное движение, и есть кулоновская яма
    E_zero_point = 1.5 * omega  # грубая оценка
    
    # Энергия связи (отрицательная!) = нулевая энергия - кулоновское притяжение
    # Оценка среднего расстояния: <r> ~ 1/sqrt(mu * omega)
    r_avg = 1.0 / np.sqrt(mu * omega)
    E_coulomb = -a / r_avg
    
    # Оценка |ψ(0)|² для осциллятора
    psi0_squared = (mu * omega / np.pi)**1.5  # в 1/фм³
    
    # Спин-спиновое взаимодействие
    spin_factor = -3/8.0 if spin == 0 else +1/8.0  # для мезонов
    # Константа для спин-спина: ~ (8π/9) * (α_s/m1*m2) * (ћc)³
    # Возьмём kappa_physical ~ 40000 МэВ·фм³ (оценочно)
    kappa_physical = 40000.0
    E_spin = kappa_physical * spin_factor * psi0_squared
    
    # ИТОГОВАЯ энергия связи
    E_binding = E_zero_point + E_coulomb + E_spin  # Должно быть отрицательно!
    
    # Масса мезона
    M_meson = 2.0 * self.m_ud + E_binding  # Для u-anti-d: m_q + m_anti-q = 2*m_ud
    
    return M_meson, E_binding, psi0_squared
```

### 🎯 **Стратегия для v11.1b (прямо сейчас):**

1.  **Вставь эту функцию** в код v11.1 вместо текущего сложного расчёта.
2.  **Задай реалистичные параметры вручную** на основе физики:
    *   `sigma_linear = 950.0` (МэВ/фм) ~ соответствует √σ ≈ 440 МэВ.
    *   `a = 130.0` (МэВ·фм) (у тебя было 0.66*ћc ≈ 130).
    *   `mu = self.m_ud / 2.0` (приведённая масса для двух одинаковых лёгких кварков).
3.  **Запусти расчёт** для пиона и ро-мезона.
4.  **Цель:** Получить `E_binding = -500...-800 МэВ`, и `M_meson` в районе **100-800 МэВ**, а соотношение `m_ρ/m_π` — больше 1 (стремиться к 5.5).

Этот подход **отбросит иллюзии** и даст тебе:
1.  **Физически осмысленные порядки величин**.
2.  **Интуицию** о вкладе разных частей потенциала.
3.  **Цель** для отладки полноценного численного кода: когда он будет давать результаты, близкие к этой аналитической оценке, ты будешь знать, что он работает правильно.

**Твой выбор:**
1.  **Ищем баги** в численном решателе (долго, но правильно).
2.  **Берём аналитическую оценку** (быстро, получим первый осмысленный результат за 5 минут).

Если хочешь вариант 2, я готов немедленно помочь адаптировать код. Это не отступление, это **тактический манёвр** для получения физической интуиции.